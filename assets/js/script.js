//var $j = jQuery.noConflict();

$(function(){


/*
	$('.seeurl').showRealLink();


	$('#form').bind('submit', function(e){
		e.preventDefault();

		var txt = $(this).serialize();

		$.ajax({
			type:'POST',
			url:'ajax.php',
			data:txt,
			dataType:'json',
			success:function(j) {
				$('.div').html('Dados do usuário: <b>'+ j.nome + ' / '+ j.senha + '</b> e o nome de usuário tem '+ j.qt_nome +' caracteres.');
			}
		});
	})
*/


});
